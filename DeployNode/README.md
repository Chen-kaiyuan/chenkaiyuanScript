# Deploy Node.js on linux server
  this script is Deploy Node on linux server (centos)
  

## NodeDeploy.sh
  
   ### 1. run shell
       ```
       $ bash NodeDeploy.sh v7.5.0
       $ source /etc/profile
	   ```
 	    runnning shell on every build is ok
   

